server {
    listen       80;

    location / {
        root /var/local/art12/art12/static;
        sendfile on;
        tcp_nopush on;
        gzip on;
        gzip_types text/plain application/x-javascript text/css;
        expires 1d;
    }
}
